// DELAY parameters

$: s("cp").delay(0.5).delaytime(0.2).delayfeedback(0.1).bank("RolandTR808")

// delay(0.5) = wet/dry mix
// delaytime(0.2) = echo spacing
// delayfeedback(0.7) = how much feeds back (more = more echoes)

// Try changing delayfeedback: 0.3 vs 0.9
